import type { ShoppingListItemDTO } from "@/types";

/**
 * Low Stock Item ViewModel
 *
 * Represents a low-stock item displayed in the home dashboard.
 * This is an alias of ShoppingListItemDTO as no transformation is needed
 * for the home view.
 */
export type LowStockItemViewModel = ShoppingListItemDTO;
